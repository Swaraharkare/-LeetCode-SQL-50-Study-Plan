/* Write your T-SQL query statement below */

WITH CTE AS(SELECT DISTINCT product,sell_date FROM Activities)

SELECT sell_date,
COUNT( DISTINCT product) as num_sold,
STRING_AGG( product,',')WITHIN GROUP (ORDER BY product ASC) AS products                     
FROM CTE 
GROUP BY sell_date  ;
